% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ltx_fontsize.R
\name{ltx_fontsize}
\alias{ltx_fontsize}
\title{Set Font Size for LaTeX Table}
\usage{
ltx_fontsize(x, font_size = 8, skip = NULL, print_tbl = FALSE, verbose = TRUE)
}
\arguments{
\item{x}{A character vector representing LaTeX code for a table, typically
captured via \code{ltx_capture_output()}.}

\item{font_size}{Numeric value indicating the desired font size
(default is 8).}

\item{skip}{Numeric value for the line spacing (skip), typically set to 1.2
times the font size. If \code{NULL}, it defaults to 1.2 * \code{font_size}.}

\item{print_tbl}{Logical, whether to print the modified LaTeX table to the
console (default is \code{FALSE}).}

\item{verbose}{Logical, whether to display informative messages via the \code{cli}
package (default is \code{TRUE}).}
}
\value{
Returns the modified LaTeX code as a character vector (invisible).
}
\description{
This function modifies the LaTeX code for a table to set the font size and
line spacing. It appends the necessary LaTeX command to the table's LaTeX
code.
}
\details{
The function appends a LaTeX \verb{\\\\fontsize\{\}} and \verb{\\\\selectfont} command
directly after the \verb{\\\\begin\{table\}} tag in the input LaTeX code. If the
\code{skip} parameter is \code{NULL}, the function sets the skip value to 1.2 times
the \code{font_size}.

The function validates that \code{font_size} and \code{skip} are positive values and
that a \verb{\\\\begin\{table\}} tag is present. If verbose is enabled, informative
messages are displayed to guide the user.
}
\examples{
\dontrun{
# Sample LaTeX table code
table_code <-
c("\\\\begin{table}",
"\\\\centering",
"\\\\begin{tabular}{cc}",
"A & B \\\\\\\\",
"1 & 2 \\\\\\\\",
"\\\\end{tabular}",
"\\\\end{table}")

# Modify font size to 10
modified_code <- ltx_fontsize(table_code, font_size = 10, skip = 12, print_tbl = TRUE)
}
}
\seealso{
\code{ltx_capture_output}
}
